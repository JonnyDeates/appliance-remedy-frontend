import { render, screen} from '@testing-library/react';

describe('ContactForm.js',() => {
    it('Should render contact form for the contact page page.', ()=>{
        //Assign
        render(<ContactForm/>);

        const contactForm = screen.getByRole

        // Assessment


    })
});